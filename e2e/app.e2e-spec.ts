import { Ng4myCalcPage } from './app.po';

describe('ng4my-calc App', () => {
  let page: Ng4myCalcPage;

  beforeEach(() => {
    page = new Ng4myCalcPage();
  });

  /*it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('app works!');
  });*/
});
